/**
 * OperatorPage.tsx — placeholder operator dashboard route (feature 052/064).
 *
 * Provides a routed entry-point for the operator dashboard (feature 064).
 * Renders a minimal shell until that feature ships.
 */
export function OperatorPage() {
  return (
    <div className="flex h-screen flex-col items-center justify-center gap-4 p-8 text-center">
      <h1 className="text-2xl font-bold">Operator Dashboard</h1>
      <p className="max-w-sm text-sm text-muted-foreground">
        The operator dashboard is coming in feature 064. This route is reserved.
      </p>
    </div>
  );
}
