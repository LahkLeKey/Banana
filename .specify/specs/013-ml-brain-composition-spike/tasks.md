# Tasks: ML Brain Composition & Productionization SPIKE

**Branch**: `013-ml-brain-composition-spike` | **Created**: 2026-04-26

SPIKE-only. No implementation tasks. All tasks are documentation under
`.specify/specs/013-ml-brain-composition-spike/`.

## Phase 1 -- Decision artifacts

- [ ] T001 Author `analysis/composition-strategy.md` expanding R-01 with
  worked walks for each 010/011/012 anchor under the gated-cascade strategy.
- [ ] T002 Author `analysis/consumer-mapping.md` expanding R-02 with concrete
  file paths (`src/c-sharp/asp.net/Pipeline/**`, `src/typescript/react/src/**`,
  `src/typescript/electron/**`) and the owning helper agent per row.
- [ ] T003 Author `analysis/deferred-registry.md` capturing the U-001 trigger
  and dropping the 009-era pairwise-secondary-edges item.

## Phase 2 -- Follow-up readiness packets

- [ ] T004 Draft `analysis/followup-A-aspnet-ensemble.md` per the readiness
  packet template (gated cascade in ASP.NET pipeline, no new native exports).
- [ ] T005 Draft `analysis/followup-B-react-ensemble-badge.md` (React
  `BananaBadge` ensemble variant + optional attention chip behind feature flag).
- [ ] T006 Draft `analysis/followup-C-quantized-embedding.md` (deferred with
  trigger from R-03; packet exists so the slice is plan-ready when triggered).

## Phase 3 -- Tracker + close-out

- [ ] T007 Author `README.md` execution tracker with the recommended slice
  order and links to all phase 1+2 artifacts.
- [ ] T008 Cross-link `analysis/deferred-registry.md` from
  `012-full-brain-transformer-runnable/analysis/residual-followups.md`
  (one-line pointer; no behavior change to 012 evidence).
- [ ] T009 Mark all 9 SPIKE tasks complete; record SPIKE close-out note in
  `README.md`.

## Out of scope

- No native code changes (`src/native/**` read-only).
- No managed code changes (`src/c-sharp/**` read-only).
- No JS/TS code changes (`src/typescript/**` read-only).
- No `.specify/feature.json` repointing.
