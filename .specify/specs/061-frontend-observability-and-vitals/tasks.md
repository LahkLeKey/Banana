# Tasks — 061 Frontend Observability and Vitals

- [x] T001 — Install `web-vitals` and wire `onCLS`, `onINP`, `onLCP` reporters
- [x] T002 — Send vitals to API `/metrics/vitals` POST endpoint
- [x] T003 — Add `ErrorBoundary` component wrapping `<App>` with error reporting
- [x] T004 — Add `window.onerror` + `unhandledrejection` global handlers
- [x] T005 — Display last-reported LCP in operator dashboard panel
- [x] T006 — CI: verify vitals reporter is included in production bundle
