# 029 — Plan

Schema: insert `checkpointAuroraLaneTag` between meridian and objectiveCompletionIds.
Service: add `normalizeCheckpointReconciliationLaneTag`; extract+include aurora in signature; reject reconciliation-lane after convergence-lane.
Tests mirror 028 with reconciliation/aurora lanes.
