# 027 Handoff

Canonical `checkpointReplayPhaseTag` + additive `checkpointHorizonLaneTag` shipped. API 55/27, native 4/4.

## Next (028)

Canonical `checkpointConvergenceLaneTag` (diagnostic `persistent_world_continuity_checkpoint_convergence_lane_mismatch`) + additive `checkpointMeridianLaneTag` default `'meridian:lane-default'`.
