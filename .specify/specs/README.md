# Spec Root Reset

This directory was intentionally reset to remove legacy and out-of-scope spec drift.

Use Specify CLI to add only active, implementation-bound specs for the React portal game-client pivot.
