# Tasks — 064 Frontend Operator Dashboard

- [x] T001 — Create `OperatorPage.tsx` with placeholder shell (route `/operator`)
- [x] T002 — Add model release summary panel (latest active release + version)
- [x] T003 — Add background job queue status panel (pending/running/failed counts)
- [x] T004 — Add webhook subscriber list panel with active count
- [x] T005 — Add system health widget showing API + native status
- [x] T006 — Restrict page to `role === 'operator'` via `ProtectedRoute`
