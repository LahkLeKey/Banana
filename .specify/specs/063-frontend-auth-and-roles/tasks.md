# Tasks — 063 Frontend Auth and Roles

- [x] T001 — Add `useAuth` hook backed by `localStorage` JWT + expiry check
- [x] T002 — Create `AuthContext` provider wrapping the router
- [x] T003 — `ProtectedRoute` component redirects unauthenticated users to `/login`
- [x] T004 — Wire `/login` route with username + API-key form
- [x] T005 — Restrict operator routes behind `role === 'operator'` check
- [x] T006 — Add logout button that clears auth state and redirects to `/login`
