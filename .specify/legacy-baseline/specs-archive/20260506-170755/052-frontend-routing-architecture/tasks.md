# Tasks — 052 Frontend Routing Architecture

- [x] T001 — Install `react-router-dom` and create `src/lib/router.tsx` with `createBrowserRouter`
- [x] T002 — Define route tree: `/` (ClassifyPage), `/operator` (OperatorPage), `*` (NotFoundPage)
- [x] T003 — Add `ProtectedRoute` component wrapping authenticated routes
- [x] T004 — Wire `<RouterProvider>` into `main.tsx`
- [x] T005 — Add `useNavigate`/`useLocation` hooks where needed
- [x] T006 — Verify `NotFoundPage` renders for unknown paths
