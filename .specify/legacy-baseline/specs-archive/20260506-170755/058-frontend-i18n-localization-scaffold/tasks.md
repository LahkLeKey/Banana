# Tasks — 058 Frontend i18n Localization Scaffold

- [x] T001 — Install `react-i18next` and `i18next`
- [x] T002 — Create `src/i18n.ts` with `en` locale namespace loaded from `public/locales/en/translation.json`
- [x] T003 — Replace all hardcoded UI strings with `t('key')` calls
- [x] T004 — Add `<I18nextProvider>` wrapper in `main.tsx`
- [x] T005 — Add language switcher to operator panel (en / es placeholder)
- [x] T006 — CI: verify no raw string literals remain in `src/pages` and `src/components`
