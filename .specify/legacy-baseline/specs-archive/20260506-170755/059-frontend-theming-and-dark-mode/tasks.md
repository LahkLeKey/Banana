# Tasks — 059 Frontend Theming and Dark Mode

- [x] T001 — Add `class` dark mode strategy to `tailwind.config.ts`
- [x] T002 — Create `useDarkMode` hook that reads `prefers-color-scheme` and persists to `localStorage`
- [x] T003 — Apply `dark:` variants to all Tailwind utility classes in components
- [x] T004 — Add dark/light toggle button to app header
- [x] T005 — Export `--banana-*` CSS custom properties for token-based theming
- [x] T006 — Verify dark mode tokens in shared-ui `tokens/` directory
