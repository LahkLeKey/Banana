# Agent Pulse: banana-classifier-agent

Focus: not-banana feedback ingestion and model vocabulary refresh automation.

Run snapshots are written to `runs/` by `scripts/record-agent-pulse-activity.py`.
