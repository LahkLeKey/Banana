# Workflow Root Reset

This directory was intentionally reset to remove bloated and out-of-scope CI automation.

Reintroduce only minimal, implementation-critical workflows aligned to the new slim scope.
