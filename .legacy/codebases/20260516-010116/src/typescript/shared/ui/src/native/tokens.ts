// Slice 026 -- React Native re-export of the shared token contract.
// Consumers import from `@banana/ui/native/tokens`.

export type { Tokens } from "../tokens";
export { tokens } from "../tokens";
