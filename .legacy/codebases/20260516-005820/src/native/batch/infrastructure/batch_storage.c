#include "../domain/entities/batch.h"
#include <string.h>

// This file will contain infrastructure logic for persistent or shared storage if needed.
// For now, batch and harvest records are kept in static memory in the service layer.
