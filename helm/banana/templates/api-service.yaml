apiVersion: v1
kind: Service
metadata:
  name: {{ include "banana.fullname" . }}-api
  labels:
    {{- include "banana.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "banana.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
